# 改进记录（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 领域模型

- **§4.10**：**`mag_agent_improvement_log`** — `change_type`（CONFIG/PROMPT/FEEDBACK/QUALITY_TAG 等）、`summary`、`detail_json`、INSERT-only 审计型。

## 数据保留

- **§0 / §16.6 / §18**：与 `mag_message` 等一并 **永久保留**（不自动 TTL）。

## REST API（摘录）

- `GET /api/mag/projects/{id}/agents/{agentId}/improvements`

## §17 映射（摘录）

- **§17.3**「改进记录」行；**§17.10** 验收 6。
