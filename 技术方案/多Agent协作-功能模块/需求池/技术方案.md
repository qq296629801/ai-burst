# 需求池（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 领域模型

- **§4.6**：**`mag_requirement_pool_item`** — `state`、`revision_id`、`anchor_json`、`payload_json`、`assigned_decider_user_id`、`temporal_workflow_id`。

## Temporal

- **§6.1**：`UserApprovalWorkflow`（`req-approval-{poolItemId}`）；**§6.4**：`UserDecisionSignal`、长期未拍板提醒。

## REST API（摘录）

- `GET/POST /api/mag/projects/{id}/requirement-pool`  
- `POST /api/mag/requirement-pool/{id}/decide`

## 状态枚举

- **§19.1**：`PENDING_USER`、`CLOSED_BY_PRODUCT`、`USER_CONFIRMED_*` 等 — 须与产品 §6.3 全量对齐。

## 权限与错误码

- **§10**：`mag:pool:decide`；**§19.7**：`MAG_POOL_DECIDE_NOT_ALLOWED`、`MAG_POOL_STATE_INVALID` 等。

## §17 映射（摘录）

- **§17.3**「需求池」行；**§17.7** 产品 §6；**§17.10** 验收 9、13。
