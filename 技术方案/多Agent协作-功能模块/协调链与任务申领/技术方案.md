# 协调链与任务申领（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 领域模型

- **§4.3**：**`mag_module`**（功能模块树）；**`mag_task`**（`module_id`、`state`、`assignee_agent_id`、`block_reason` 等）；**§4.3.1** 任务状态枚举与产品 §5.2 对照。

## REST API（摘录）

- `GET/POST /api/mag/projects/{id}/tasks`  
- `GET/POST /api/mag/projects/{id}/modules`  
- `POST /api/mag/tasks/{id}/request-next` — 子 Agent **要活**

## 编排要点

- **§5.2**：PM/主 Agent 工具（`assignTask` 等）与任务表更新；消息层 `kind` 约定见 **§17.5**。

## §17 映射（摘录）

- **§17.3**「协调链」「任务与申领」行；**§17.5** §4.1～4.2；**§17.10** 验收 3。
