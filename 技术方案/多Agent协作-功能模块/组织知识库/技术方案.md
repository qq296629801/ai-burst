# 组织知识库（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 领域模型

- **§4.7**：**`mag_kb_entry`** — `source`（ARCHIVE_REFLOW/MANUAL）、`title`、`body`、`tags_json`、`keywords`、`archive_id`、FULLTEXT(title,body)。

## 检索与回流

- **§16.1**：`quality_flag=1` 自动回流 `mag_kb_entry`；首期 FULLTEXT + 标签/关键词；配置 `aiburst.mag.kb.reflow-mode`。

## REST API（摘录）

- `GET/POST /api/mag/kb/entries`、`GET/PUT/DELETE /api/mag/kb/entries/{id}`

## Agent 工具

- **§5.2**：`searchOrgKb`（SQL LIKE + FULLTEXT）。

## 权限

- **§10**：`mag:kb:manage`；蓝图导入权限见 **§17.8** 与 `mag:kb:blueprint:import` 二选一说明。

## §17 映射（摘录）

- **§17.3**「组织知识库」行；**§17.11** 产品 §11 知识库实现细节已定稿项。
