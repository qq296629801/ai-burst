# 项目与成员（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 领域模型

- **§4.1 项目与成员**：**`mag_project`**（名称、状态、`current_req_doc_id`、`config_json`）；**`mag_project_member`**（`user_id`、`project_id`、`role_in_project`：OWNER/MEMBER/VIEWER）。

## REST API（摘录）

- `GET/POST /api/mag/projects`、`GET/PUT/DELETE /api/mag/projects/{id}`  
- `GET/POST /api/mag/projects/{id}/members`

## 权限与菜单

- **§10**：`mag:project:list`、`mag:project:manage` 等；**§10.1** 项目成员角色默认矩阵。

## 前端

- **§11**：`/mag/projects`、`/mag/project/:id` 工作台 Tab（成员等）。

## §17 映射（摘录）

- **§17.3**「项目与成员视图」行；**§17.6** §5.1 列表聚合字段。
