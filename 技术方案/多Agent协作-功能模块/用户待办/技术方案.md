# 用户待办（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 读模型与 API

- **§16.2 待办与需求池读模型**：`GET /api/mag/todos` 聚合 `mag_requirement_pool_item.state = PENDING_USER`，过滤：项目成员 + `mag:pool:decide` + `assigned_decider_user_id` 规则（含 OWNER 例外）。
- **§9**：`GET /api/mag/todos`

## 关联表

- **§4.6**：`mag_requirement_pool_item`（待拍板项载体，与需求池模块共用）。

## 前端

- **§11**：`/mag/todos`；与需求池 Tab **双向跳转**（产品 §5.6.2）。

## §17 映射（摘录）

- **§17.3**「用户待办」行；**§17.2** 目标 10。
