# 定时任务（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 领域模型

- **§4.9**：**`mag_scheduled_job_config`** — `job_key`、`cron_expr`、`enabled`、`project_id` 可空、`last_run_at`。

## 执行与锁

- **§7 定时任务与 Redis 分布式锁**：Key `mag:sched:{jobKey}`；典型 job：停摆扫描、待拍板超时提醒等。

## REST API（摘录）

- `GET/PUT /api/mag/scheduled-jobs`（权限种子见完整版 §9 注释）

## §17 映射（摘录）

- **§17.3**「定时任务」行；**§17.10** 验收 7；**§17.9** 可靠性（失败 → `mag_alert_event`）。
