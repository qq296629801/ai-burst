# 阻塞与升级链路（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 领域模型

- **`mag_task`**：`block_reason`、`blocked_by_agent_id`（§4.3）。  
- **`mag_pm_assist_record`**、**`mag_alert_event`**（§4.5）。  
- **`mag_external_fetch_audit`**（§4.8）— 外网检索留痕。  
- **`mag_message`** — 结构化阻塞 `kind: BLOCK`（§17.5）。

## Temporal（可选）

- **§6.1**：`escalate-{threadId}` — 阻塞升级链路与 SLA 超时合并（可选）。

## REST API（摘录）

- `POST /api/mag/tasks/{id}/block`  
- `GET /api/mag/projects/{id}/pm-assist`

## Agent 工具与检索

- **§5.2**：`fetchUrl` 代理出站 + SSRF 黑名单 + 全量审计（**不做 URL 白名单**，见 §0/§12）。  
- **§16.5**：外网检索失败处理摘要。

## §17 映射（摘录）

- **§17.3**「阻塞与升级链路」行；**§17.6** §5.6～5.6.1；**§17.10** 验收 11、12。
