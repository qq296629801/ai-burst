# 运营大屏（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## WebSocket

- **§8**：路径 `/ws/mag`、JWT 握手、`SUBSCRIBE project:{id}` / `org`；事件 `task.state.changed`、`alert.new`、`agent.heartbeat` 等；**§8.1** 消息信封 JSON 草案。

## 快照与告警 API

- **§9**：`GET /api/mag/dashboard/snapshot`、`GET /api/mag/projects/{id}/alerts`、`POST /api/mag/alerts/{id}/ack`

## 领域模型

- **§4.5**：**`mag_alert_event`** — 大屏/通知持久化。

## 指标与通知

- **§16.3**：WS + `mag_alert_event` + QQ 机器人可选；**§16.4**：产出效率数据源（任务状态时间戳为主）。

## 前端

- **§11**：`/mag/dashboard/:projectId?`、`useMagWebSocket()`。

## §17 映射（摘录）

- **§17.3**「运营大屏」行；**§17.6** §5.5；**§17.10** 验收 10。
