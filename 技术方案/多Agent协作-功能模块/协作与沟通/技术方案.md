# 协作与沟通可观测（技术方案节选）

> 完整版：[多Agent协作技术方案.md](../../多Agent协作技术方案.md)

## 领域模型

- **§4.2**：**`mag_thread`**（`project_id`、`task_id` 可空、标题）；**`mag_message`**（`thread_id`、`sender_type` USER/AGENT/SYSTEM、`sender_agent_id`、`content` MEDIUMTEXT）。

## REST API（摘录）

- `GET/POST /api/mag/projects/{id}/threads`  
- `GET/POST /api/mag/threads/{id}/messages`  
- 列表带 query：`agentId`、`from`、`to`（实现期与 OpenAPI 对齐）

## 安全

- **§12**：按角色脱敏；所有查询带 `project_id` 与成员校验。

## §17 映射（摘录）

- **§17.3**「协作与沟通可观测」行；**§17.5** §4.4 线程时间线与 `@Agent`。
