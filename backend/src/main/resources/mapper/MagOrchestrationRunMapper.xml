<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiburst.mag.mapper.MagOrchestrationRunMapper">

    <resultMap id="BaseMap" type="com.aiburst.mag.entity.MagOrchestrationRun">
        <id column="id" property="id"/>
        <result column="project_id" property="projectId"/>
        <result column="run_kind" property="runKind"/>
        <result column="agent_id" property="agentId"/>
        <result column="thread_id" property="threadId"/>
        <result column="workflow_id" property="workflowId"/>
        <result column="status" property="status"/>
        <result column="message" property="message"/>
        <result column="result_summary" property="resultSummary"/>
        <result column="trigger_user_id" property="triggerUserId"/>
        <result column="started_at" property="startedAt"/>
        <result column="finished_at" property="finishedAt"/>
    </resultMap>

    <select id="selectById" resultMap="BaseMap">
        SELECT * FROM mag_orchestration_run WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mag_orchestration_run (
            project_id, run_kind, agent_id, thread_id, workflow_id, status, message, result_summary,
            trigger_user_id, started_at, finished_at
        ) VALUES (
            #{projectId}, #{runKind}, #{agentId}, #{threadId}, #{workflowId}, #{status}, #{message},
            #{resultSummary}, #{triggerUserId}, #{startedAt}, #{finishedAt}
        )
    </insert>

    <select id="selectByWorkflowId" resultMap="BaseMap">
        SELECT * FROM mag_orchestration_run WHERE workflow_id = #{workflowId}
    </select>

    <update id="updateRunning">
        UPDATE mag_orchestration_run SET status = 'RUNNING' WHERE workflow_id = #{workflowId}
    </update>

    <update id="updateFinished">
        UPDATE mag_orchestration_run
        SET status = #{status},
            result_summary = #{resultSummary},
            finished_at = #{finishedAt}
        WHERE workflow_id = #{workflowId}
    </update>

    <select id="selectByProjectId" resultMap="BaseMap">
        SELECT * FROM mag_orchestration_run
        WHERE project_id = #{projectId}
        ORDER BY started_at DESC
        LIMIT #{limit}
    </select>
</mapper>
