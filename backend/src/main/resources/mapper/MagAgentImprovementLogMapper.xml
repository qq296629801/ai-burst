<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiburst.mag.mapper.MagAgentImprovementLogMapper">

    <resultMap id="BaseMap" type="com.aiburst.mag.entity.MagAgentImprovementLog">
        <id column="id" property="id"/>
        <result column="project_id" property="projectId"/>
        <result column="agent_id" property="agentId"/>
        <result column="change_type" property="changeType"/>
        <result column="summary" property="summary"/>
        <result column="detail_json" property="detailJson"/>
        <result column="created_by_user_id" property="createdByUserId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mag_agent_improvement_log (project_id, agent_id, change_type, summary, detail_json, created_by_user_id)
        VALUES (#{projectId}, #{agentId}, #{changeType}, #{summary}, #{detailJson}, #{createdByUserId})
    </insert>

    <select id="selectByProjectAndAgent" resultMap="BaseMap">
        SELECT * FROM mag_agent_improvement_log
        WHERE project_id = #{projectId} AND agent_id = #{agentId}
        ORDER BY id DESC
    </select>

    <select id="selectByProjectId" resultMap="BaseMap">
        SELECT * FROM mag_agent_improvement_log
        WHERE project_id = #{projectId}
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <select id="countByProjectAgentCreatedAtSince" resultType="int">
        SELECT COUNT(1) FROM mag_agent_improvement_log
        WHERE project_id = #{projectId}
          AND agent_id = #{agentId}
          AND created_at &gt;= #{sinceInclusive}
    </select>
</mapper>
